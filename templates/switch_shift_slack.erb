<% if fail_reason.nil? %>
Ok, put <%= person.name %> on <%= utilities.display_schedule schedule %> from <%= utilities.display_time from %> to <%= utilities.display_time to %>.
<% elsif fail_reason == :not_on_call %>
Sorry, but <%= shift_person.name %> is not on <%= utilities.display_schedule schedule %> on <%= Date.parse(day.to_s) %>.
<% elsif fail_reason == :on_call_multiple_times %>
Sorry, but <%= shift_person.name %> is on call multiple times on <%= Date.parse(day.to_s) %>. <%= utilities.display_schedule(schedule, 'View Schedule') %>
<% end %>
